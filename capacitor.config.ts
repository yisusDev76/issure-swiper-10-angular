import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'issure-swiper10-angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
